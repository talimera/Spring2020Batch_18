package JavaExtraPractices;

public class ThingWeDidntLearn {

    public static void main(String[] args) {

        String srt = "HelloMotto";

        System.out.println(srt.length()); // gives you length of the String ---> (variableName.length())

        System.out.println(srt.toUpperCase()); // turns your string values into uppercase ---> (variableName.toUppercase())
        System.out.println(srt.toLowerCase()); // turns your string values into lowercase ---> (variableName.toLowercse())

        System.out.println(srt.indexOf("o")); // gives you the first occurrence of specific part of String ---> (variableName.Indexof("a")) gives number
        // System.out.println(.concat()); can be use for concatenating strings
        int x = 15;

        double root=Math.sqrt(x); // Math.sqrt(variableName) ----> gives you root of the number you enter. Make sure to use it with double primitive with it

        System.out.println(root);

      Math.random();// gives you decimal number between 0(inclusive) and 1 (exclusive). Dont know the usage of it.




    }


}
